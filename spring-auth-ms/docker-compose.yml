services:
#  auth:
#    build:
#      context: .
#      dockerfile: Dockerfile
#    env_file:
#      - .env_auth
#    depends_on:
#      - db
#    ports:
#      - "8080:8080"
  db:
    image: postgres:17
    env_file:
      - .env_db
    restart:
      always
    ports:
      - "5433:5432"
    volumes:
      - db-data:/var/lib/postgresql

volumes:
  db-data:


