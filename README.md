# TrackForge

TrackForge is a backend project for project management that is still under active development.
The repository currently contains two Spring Boot services: `spring-auth-ms` for authentication and `project-core-service` for project data.
PostgreSQL is used for local development, with Docker Compose files included for the service databases.
API behavior and project structure may change while the application is being built.
